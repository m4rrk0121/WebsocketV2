cat > package.json << 'EOF'
{
  "name": "websocket-server",
  "version": "1.0.0",
  "description": "WebSocket API server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "mongodb": "^6.15.0",
    "socket.io": "^4.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3"
  }
}
EOF
